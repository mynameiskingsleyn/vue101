export default{
    methods:{
        // get(url){
        //
        // },
        // post(url,data,successMessage='success',action=''){
        //
        // },
        isEmpty(obj){
          return Object.keys(obj).length===0;
        }
    }
}
